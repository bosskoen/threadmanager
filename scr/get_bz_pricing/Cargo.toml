[package]
name = "get_bz_pricing"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["dylib"]

[dependencies]
library = {path = "../library"}
humansize = {version = "2.1.3"}
serde = {version = "1.0.216", features = ["derive"]}
serde_json = "1.0.134"
